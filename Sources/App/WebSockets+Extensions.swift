//
//  File.swift
//  
//
//  Created by Daniel Earman on 7/17/22.
//

import Vapor

extension WebSocket {
/*
  func send(_ message: Message) {
    let encoder = JSONEncoder()
    guard
      let data = try? encoder.encode(message),
      let str = String(data: data, encoding: .utf8)
    else { return }
    
    /// strings cuz easier to debug
    send(str)
  }
 */
}
